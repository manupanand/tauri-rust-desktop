# This repo contain desktop application
    -React/Next
    -Tauri
    -Postgresql/sqlite
    -Rust

```
npm create vite@latest
npm install --save-dev @tauri-apps/cli
npm run tauri init
npm run tauri dev
```